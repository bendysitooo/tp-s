#include <iostream>
using namespace std;

class Sumatoria {
private:
    int suma;

public:
    Sumatoria() {
        suma = 0;
        int valor;
        cout << "Ingrese un valor entero (0 para finalizar): ";
        cin >> valor;
        while (valor != 0) {
            suma += valor;
            cout << "Ingrese un valor entero (0 para finalizar): ";
            cin >> valor;
        }
    }
    ~Sumatoria() {
    cout << "La suma de los valores ingresados es: " << suma << endl;
    }
};

int main() {
    Sumatoria sumaValores;
    return 0;
}
